знайти максимально непарне число

const arr = ['1', '111', '110', '15', '20'];

const res = arr
  .map((el) => parseInt(el)) Преобразуємо в масив чисел
  .filter((num) => num%2) якщо остаток від ділення на 2 буде виходити 1, то залишаться в масиві лише непарні числа в данному випадку [1,111,15]
  .reduce((max, value) => Math.max(max, value), 0);
